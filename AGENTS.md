# 嵌入式项目协作规则

## 适用与优先级

- 按以下优先级处理冲突：用户明确要求、仓库现有规则与工具、邻近代码风格、本文件默认规则。
- 仅修改完成当前任务所需的内容。不要顺带重构、重命名或统一格式。
- 信息不足但可安全推断时，说明假设并继续；只有不同选择会显著改变结果或风险时才询问用户。

## 修改代码

- 修改前先检查目录结构、构建系统、相关实现、公开接口和测试。
- 优先复用已有模块与接口，保持现有架构和兼容性。
- 不要通过跨模块全局变量访问内部状态；确有既有 ABI、启动代码或链接器约束时除外。
- 涉及共享数据时明确执行上下文。`volatile` 不提供原子性、互斥或线程同步。
- ISR 只执行有界、非阻塞且平台允许的操作；把复杂处理移交任务或主循环。
- 对内存、栈、CPU、Flash、实时性有影响的修改，说明依据或保留的风险。
- 不在日志、测试输出或示例中写入密码、密钥、Token、证书或完整隐私数据。
- 新建或重构嵌入式工程时，必须以 `App/`、`BSP/`、`Drivers/`、`Middleware/`、`Platform/` 等顶层目录明确展示分层；不要把自研代码混放在厂商库目录。
- 编写自研 C/C++ 源码时，默认采用 `embedded-firmware/references/commenting.md` 规定的教学型详细中文注释；已有仓库明确采用其他规范时，先遵循仓库规范或用户要求。

## 验证与交付

- 优先运行仓库已有的格式化、构建、测试和静态检查命令。
- 不声称未实际运行的检查已经通过；无法验证时说明原因。
- 最终说明修改结果、实际验证和仍需关注的风险，不强制套用固定回答模板。

## 专项规范

- 处理 MCU、RTOS、BSP、驱动、状态机、硬件接口或嵌入式 C/C++ 时，读取并遵循 `embedded-firmware/SKILL.md`；若该 Skill 已安装，也可通过 `$embedded-firmware` 调用。
